export interface Env {
  URI_PORT: string;
  MAX_REDIRECTS: string;
  REDIRECTION_URI: string;
  TIMEOUT: string;
  CERT_SSL_PATH: string;
  KEY_SSL_PATH: string;
  CERT_SSL_PWD: string;
  PORT_SSL: string;
}
